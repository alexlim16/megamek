package megamek.server.managers;

import megamek.server.GameManager;

public class VictoryReportManager extends VictoryManager {
    public VictoryReportManager(GameManager gameManager) {
        super(gameManager);
    }
}
